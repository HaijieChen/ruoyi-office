# OA 流程优化 318 配置验收记录

> 配置依据：`docs/OA流程优化-318（合同用印流程变动）.xlsx`
> 实施分支：`codex/oa-platform-production`
> 实施日期：2026-07-18

## 1. 部署与安全基线

### 1.1 数据库备份

- 备份文件：`.codex-tmp/backups/task9-predeploy-20260718-1002.sql.gz`
- SHA-256：`d54693e6813453bb8d2c3fe64b460484f640808cf8f9c465ecc3197dadb11383`
- gzip 完整性：通过
- 表单字段扩容前增量备份：`.codex-tmp/backups/task10-pre-bpm-form-longtext-20260718-1125.sql.gz`
- 增量备份 SHA-256：`9d0b05b615d19dbf7ed0c0e9d7f4a457725dd234439e40690b2068d3ca459360`
- 增量备份 gzip 完整性：通过
- 备份文件被 Git 忽略，不进入源码仓库。

### 1.2 数据库与密钥

- 已应用 `sql/mysql/bpm_form_data_source.sql`，迁移可重复执行。
- 已应用 `sql/mysql/bpm_form_fields_longtext.sql`，将 `bpm_form.fields` 和部署快照
  `bpm_process_definition_info.form_fields` 扩容为 `LONGTEXT`；连续执行两次验证通过。
- 已创建 `ruoyi_form_reader@%`，仅具有 `ruoyi-office` 库的 `SELECT` 权限，无写入、DDL 和授权权限。
- 只读密码不写入 Git、`.env` 或 Docker 容器环境；启动时通过 `/run/secrets` 的 tmpfs 文件注入。
- 只读连接池大小为 2，连接超时 3 秒；SQL 单次最多返回 200 行，默认查询超时 3 秒。
- 平台 API 固定访问本机后端，仅放行：
  - `/admin-api/system/dept/company-simple-list`
  - `/admin-api/hrm/employee-archive/select-page`

### 1.3 当前运行环境

- 后端：`ruoyi-office-backend`，端口 `48080`，健康检查 `UP`。
- 前端：Vben 开发服务，端口 `5666`。
- 回滚容器：
  - `ruoyi-office-backend-pre-platform-api`
  - `ruoyi-office-backend-pre-task9`
- 当前后端 JAR 通过 `-Pboot` 单体模式构建；旧容器未删除。

## 2. 已发布表单数据源

所有数据源状态均为启用且已发布。SQL 数据源只允许参数化 `SELECT`/`WITH ... SELECT`，均包含租户条件。

| 编号 | Code | 类型 | 用途 | 试运行行数 | 试运行耗时 |
| ---: | --- | --- | --- | ---: | ---: |
| 1 | `system_companies` | PLATFORM_API | 当前租户启用公司 | 2 | 288 ms |
| 2 | `oa_available_seals` | SQL | 所选公司可用印章 | 0 | 125 ms |
| 3 | `bpm_approved_processes` | SQL | 当前用户已审批前置流程 | 0 | 102 ms |
| 4 | `hrm_entry_processes` | SQL | 已审批员工入职流程 | 0 | 95 ms |
| 5 | `hrm_active_employees` | PLATFORM_API | 在职员工，支持公司/部门联动 | 0 | 128 ms |
| 6 | `crm_customers` | SQL | 客户/供应商统一客商档案 | 0 | 94 ms |
| 7 | `crm_customer_addresses` | SQL | 客商地址、税务和银行信息 | 0 | 97 ms |
| 8 | `asset_employee_assets` | SQL | 员工领用资产 | 0 | 89 ms |
| 9 | `bpm_recharge_contracts` | SQL | 已审批且有效的充值合同 | 0 | 90 ms |
| 10 | `bpm_rebate_rules` | SQL | 已生效返点规则 | 0 | 110 ms |
| 11 | `bpm_recharge_records` | SQL | 可退款/转充充值记录 | 0 | 97 ms |
| 12 | `bpm_unclaimed_receipts` | SQL | 仍有余额的未认领到款 | 0 | 87 ms |
| 13 | `oa_seal_types` | DICT | `oa_seal_type` 印章类型字典 | 7 | 79 ms |

当前发布版本：`hrm_entry_processes` 为 v2、`hrm_active_employees` 为 v4、
`crm_customers` 为 v2，其余数据源为 v1。版本提升用于补全表单所需输出字段和员工搜索/分页参数，
旧版本保持不可变。

说明：数据库已按生产要求清理，除公司和基础字典外暂无业务数据，因此业务查询返回 0 行是预期结果。充值合同、返点、充值记录、到款认领没有平行新建业务表，按要求查询 Flowable 历史变量。

### 2.1 联动参数安全约定

`tenantId`、`userId`、`deptId`、`companyId` 为服务端登录上下文保留参数，浏览器不能覆盖。业务表单中用户选择的公司/部门使用：

- `selectedCompanyId`
- `selectedDeptId`

员工选择接口优先使用上述业务选择值，并继续受租户拦截器约束。这同时满足跨公司业务选择和服务端上下文防篡改。

### 2.2 字典选择器验证

- 已创建临时 `RemoteDataSourceSelect` 表单并成功通过保存期联动校验。
- 运行时接口必须提供已部署流程定义或当前审批任务上下文；仅凭表单 ID 的调用被错误码 `1009010029` 拒绝，权限边界符合设计。
- 临时表单已删除，数据库残留数量为 0。
- 试运行接口只返回 `rows`、`total`、`version`；管理精简列表不返回 SQL、参数 Schema 或结果 Schema。
- 本批 15 个流程已发布为 v1，运行时联动按真实流程定义权限校验，不使用临时流程绕过安全边界。

## 3. 自动化验证

| 验证项 | 结果 |
| --- | --- |
| 公司精简接口测试 | 1/1 通过 |
| 员工公司/部门过滤测试 | 2/2 通过 |
| BPM 后端全量测试 | 200 个执行，0 失败，0 错误，6 跳过 |
| 数据源与联动前端专项测试 | 61/61 通过 |
| Vben 生产构建 | Vite 构建成功，仅有仓库既有跨模块/分包警告 |
| 超过 5000 字符的动态表单持久化回归测试 | 1/1 通过 |
| 单体后端完整打包 | 42/42 Maven 模块成功 |
| 后端健康检查 | `UP` |
| 发起流程页验收 | 14 个业务流程全部可见，隐藏编排父流程不展示 |
| 数据源发布指针一致性 | 13/13 |

全量前端 `vue-tsc` 仍被仓库已有的资产、WMS、AI、IoT 等跨模块类型错误阻断；本次数据源、远程选择器和联动文件未出现在错误列表中。专项测试与实际页面加载均通过。

另修正一条旧品牌测试断言：测试输入已是“宇擎源码”，脱敏期望仍写成“芋***”；现已改为“宇***”，未修改脱敏实现。

## 4. 动态表单

已根据 Excel 配置并启用 14 个动态表单。每个表单创建后均重新读取，字段数量、数据源引用和
安全属性逐项核对；14/14 通过，未持久化 URL、SQL、请求方法、解析函数或转换脚本。

| 表单 ID | 表单名称 | 顶层字段数 | 关联数据源 |
| ---: | --- | ---: | --- |
| 36 | 入职申请 | 27 | `system_companies` |
| 37 | 离职申请 | 24 | `system_companies`、`hrm_active_employees`、`asset_employee_assets` |
| 38 | 采购申请 | 28 | `system_companies`、`crm_customers` |
| 39 | 普通用印 | 18 | `system_companies`、`oa_available_seals`、`crm_customers`、`crm_customer_addresses` |
| 40 | 业务合同审批 | 32 | `system_companies`、`oa_available_seals`、`crm_customers`、`crm_customer_addresses`、`bpm_approved_processes` |
| 41 | 员工劳动合同 | 24 | `system_companies`、`hrm_entry_processes`、`oa_available_seals` |
| 42 | 付款申请 | 35 | `system_companies`、`crm_customers`、`bpm_approved_processes` |
| 43 | 费用报销 | 21 | `system_companies`、`hrm_active_employees` |
| 44 | 开票申请 | 33 | `system_companies`、`crm_customers` |
| 45 | API 开票申请 | 23 | `system_companies` |
| 46 | 返点比例审批 | 28 | `system_companies`、`crm_customers`、`bpm_recharge_contracts` |
| 47 | 充值申请 | 35 | `system_companies`、`crm_customers`、`bpm_recharge_contracts`、`bpm_rebate_rules` |
| 48 | 退款/转充 | 27 | `system_companies`、`crm_customers`、`bpm_recharge_records` |
| 49 | 到款认领/确认 | 27 | `system_companies`、`crm_customers`、`bpm_unclaimed_receipts`、`bpm_recharge_records` |

已配置的主要联动包括公司到印章、公司到员工、客户到地址、客户到合同、合同到账户返点规则、
客户到充值记录、公司到未认领到款，以及数据源结果快照和只读输出字段。实体交付、前置流程、
替票报销、退款/转充类型等条件区块由表单控制规则负责显示；审批人、提醒和审批后动作仍由流程设计器负责。

## 5. 简易流程

8 个线性审批、条件分支和表单触发器为主的流程使用简易流程设计器。模型重新读取、
节点名称、表单关联、字段权限和占位审批人逐项校验通过，均已成功发布为 v1。

| 模型 ID | Key | 流程/表单 | 人工节点 | 状态 |
| --- | --- | --- | ---: | --- |
| `e9d1e3bf-825a-11f1-a036-2eb2efa24338` | `oa_employee_entry` | 入职申请 / 36 | 3 | v1 已发布 |
| `e9dd7c82-825a-11f1-a036-2eb2efa24338` | `oa_purchase_apply` | 采购申请 / 38 | 4 | v1 已发布 |
| `e9e78ea5-825a-11f1-a036-2eb2efa24338` | `oa_seal_general` | 普通用印 / 39 | 3 | v1 已发布 |
| `e9f152a8-825a-11f1-a036-2eb2efa24338` | `oa_payment_apply` | 付款申请 / 42 | 4 | v1 已发布 |
| `e9fb16ab-825a-11f1-a036-2eb2efa24338` | `oa_expense_reimbursement` | 费用报销 / 43 | 4 | v1 已发布 |
| `ea0501be-825a-11f1-a036-2eb2efa24338` | `oa_rebate_approval` | 返点比例审批 / 46 | 3 | v1 已发布 |
| `ea13f5e1-825a-11f1-a036-2eb2efa24338` | `oa_recharge_apply` | 充值申请 / 47 | 3 | v1 已发布 |
| `ea1d6bc4-825a-11f1-a036-2eb2efa24338` | `oa_refund_transfer` | 退款/转充 / 48 | 6 | v1 已发布 |

## 6. BPMN 流程

6 个需要并行网关、事件网关、计时/消息事件、多实例或回滚路径的业务流程使用 BPMN 设计器；
充值业务编排作为第 7 个 BPMN 模型，通过 Call Activity 关联已发布的子流程，且对普通用户隐藏。
七份 XML 均通过 XML 语法校验和 Flowable 实际部署解析，已发布为 v1。

| 模型 ID | Key | 流程/表单 | 人工节点 | 设计要点 | 状态 |
| --- | --- | --- | ---: | --- | --- |
| `ea286847-825a-11f1-a036-2eb2efa24338` | `oa_employee_resignation` | 离职申请 / 37 | 6 | 并行确认、30/15/7 天提醒与跳过分支 | v1 已发布 |
| `ea32c889-825a-11f1-a036-2eb2efa24338` | `oa_business_contract` | 业务合同审批 / 40 | 5 | 合同审批、用印、提醒子流程 | v1 已发布 |
| `ea3e614b-825a-11f1-a036-2eb2efa24338` | `oa_labor_contract` | 员工劳动合同 / 41 | 3 | 入职前置、合同、用印与执行 | v1 已发布 |
| `ea49abed-825a-11f1-a036-2eb2efa24338` | `oa_invoice_apply` | 开票申请 / 44 | 4 | 事件网关、开票回执、计时循环 | v1 已发布 |
| `ea51c23f-825a-11f1-a036-2eb2efa24338` | `oa_api_invoice_apply` | API 开票申请 / 45 | 2 | 消息开始/结束事件和 API 开票办理 | v1 已发布 |
| `ea59ffa1-825a-11f1-a036-2eb2efa24338` | `oa_receipt_claim` | 到款认领/确认 / 49 | 2 | 多实例认领、财务确认、撤销回滚 | v1 已发布 |
| `ea626413-825a-11f1-a036-2eb2efa24338` | `oa_recharge_orchestration` | 充值业务编排 / 47 | 0 | 子流程调用和消息事件，隐藏父流程 | v1 已发布 |

### 6.1 占位审批人

- 连同简易流程，共 52 个人工审批/办理节点统一使用 `USER(30)` 策略，参数为 `1`，即系统管理员 `admin`。
- 这是为保证模型可部署、可联调而设置的占位审批人，不代表生产职责分配。
- **正式上线前，必须在流程设计器中按真实职责替换所有 `admin` 占位节点，然后重新发布新版本。**

## 7. 回滚

1. 在平台停用相关数据源，阻止新表单运行时调用。
2. 将后端切换到 `ruoyi-office-backend-pre-platform-api`，恢复此前 JAR。
3. 如需回滚数据库，先停写，再使用第 1.1 节备份恢复 BPM、Flowable、菜单及数据源表。
4. 本批流程是新增的 v1 定义，不覆盖原有 OA 流程；回滚时先挂起或停用本批定义，确认无运行中实例后再清理模型及部署记录。
