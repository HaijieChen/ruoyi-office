# 修复前白盒用例与验收计划

基线 ebf954c2320a040569efdb335373dc5056626131。用户授权本会话直接实现，未授权执行生产历史恢复。

1. 完成请假 key17，创建用印 key17：历史流程、任务、变量、审批人记录全部保留；跨业务历史查询仍独立。
2. 同业务 key17 驳回/撤回后重提，旧历史保留，新实例引用写回业务单；不能因隐藏重复显示物理删除历史。
3. 两个租户相同 key，创建新实例不得读取或删除其他租户历史。创建校验失败不应影响任何历史。
4. 我的申请 /page：即使管理员也按当前登录人过滤；传入查询参数不能替换可信 userId。
5. 报表 /report-page：仍要求 bpm:oa-leave:query；ALL 可查询租户内所有申请；SELF 仅本人；部门范围仅对应部门员工，可与 SELF 合并；空范围返回空，不能省略过滤后返回所有。
6. 分页、状态、类型、原因、日期过滤在本人和报表入口均保留；现有页面无后端导出入口，不能凭空宣称导出验收。
7. 真实 Flowable + 服务启动路径测试验证保留历史；范围服务测试验证查询输入与拒绝空范围；检查 SQL 谓词以及 HTTP 权限声明。
8. 审计全仓 deleteHistoricProcessInstance、deleteProcessInstance、模型清理、定时/级联调用；标记有意管理操作及租户风险。
9. 生产 binlog 只读逐事务核对历史删除与新建业务类型，保存精简清单；恢复仅做日志完整性、主键冲突、关联表覆盖与隔离验证，不执行生产恢复，不触发业务监听器。
