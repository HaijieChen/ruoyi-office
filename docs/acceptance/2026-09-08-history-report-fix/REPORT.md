# 审批历史误删与请假报表修复 / 2026-09-08

## 状态

用户要求本会话直接实现。代码基于生产对应 Codeup main `ebf954c2320a040569efdb335373dc5056626131`，分支 `codex/history-report-fix-20260908`。本次没有发布生产、没有恢复生产数据。修复、测试和隔离恢复评估已完成，真实用户页面验收待部署后进行。

## 代码修复

- 删除创建流程时的 `deleteHistoricalProcessInstancesByBusinessKey`。数字业务 ID 跨表重复，不能作为清理历史的依据；同业务重提也保留历史审批记录。新流程照常生成独立实例，业务单维持现有实例引用更新逻辑。
- 新增 `/bpm/oa/leave/report-page`，保留 `bpm:oa-leave:query` 功能权限。范围来自 PermissionApi：ALL 为租户内所有，SELF 为本人，部门按部门用户集合，可合并 SELF；空/缺失范围返回空，权限服务异常不降级到全部。
- `/page` 仍只查询当前登录人的申请。现有请假报表页面改用 report-page，返回申请人 userId，取消/重新发起按钮仅本人显示；取消请求改传真实流程 ID。
- 模型清理是单独的管理操作，保留 `bpm:model:clean` 和模型管理员校验；补充模型所属租户、流程/历史/任务租户过滤及事务，避免同 key 跨租户清理或部分提交。

## 生产影响面（只读快照）

检查数据库当前两个 binlog，时间覆盖 2026-08-13 至 2026-09-08 09:41:57（中国时间）。不是对该时间范围之外的历史作保证。

| 被删业务 | 数量 | 内部单据 ID |
|---|---:|---|
| 请假 | 6 | 14、15、16、17、18、19 |
| 出差 | 3 | 10、11、12 |
| 用印 | 3 | 11、13、14 |
| 开票申请 | 1 | 48 |

逐流程编号、定义及删除日志位置见 `deleted-processes.json`。12 次流程删除可对应同一事务中新建的另一业务同编号流程（其中一个事务删两条历史）；出差 ID12 的删除与用印 ID12 的创建在相邻事务、同秒，符合相同调用链，但不将相邻事务说成同一事务。

生产 21 张含 process_instance_id 的表交叉核对：上述 13 张业务单的引用找不到运行或历史流程；另有 1 条开票抄送引用同一已删开票流程。其余检查的业务表未发现此类缺失。该检查不等于所有表/所有历史均无问题。

靳江 leave17：部门任务 assignee674（沈业然），完成时间 2026-09-04 15:50:18.179；流程结束 2026-09-07 14:03:37.451；删除 2026-09-07 15:34:44，与用印17创建同一事务。

## 恢复评估与隔离验证

未修改生产。只读复制日志后，在无网络、无端口暴露的专用 MySQL 8.0.33 容器进行测试：

1. 只选择上述删除事务中的历史删除事件，不倒放整个事务，不还原业务表/运行中任务。保留原二进制行值，将 DELETE_ROWS 事件转为 WRITE_ROWS，重新计算事件校验；隔离库使用生产表结构。
2. 成功重建 7 张表共 **773 行**：流程13、任务35、活动109、变量485、审批意见25、身份关联96、二进制变量10。
3. 任务、活动、变量的流程关联检查均无孤儿；变量所引二进制对象无缺失。10 个二进制对象均不引用部署资源。
4. 生产对全部773个主键逐表查询，冲突数均为0；13个流程引用的4个定义仍在生产存在。
5. 导出完整列名、hex-blob、纯 INSERT 候选 SQL（773条，不含DROP/DELETE/REPLACE），在第二个独立空库再次导入成功。沈业然原审批任务原ID、原完成时间已在隔离库核对。

候选 SQL SHA256：`0892009498b66ad00bad6b1df4273877d254a770c582efe766e160566a2dee01`。

原日志、行值和候选SQL含业务数据，未提交Git。保存在本机私有目录 `/Users/chenhaijie/.local/state/oa-history-audit-20260908`，目录仅当前用户可访问。

### 实际恢复前的门槛

先部署防误删修复；重新备份相关表并复核773个主键、业务实例引用、4个定义和当前租户；停止并发写入这些历史记录，使用事务按已验证候选集合插入，遇冲突回滚，不能 INSERT IGNORE/REPLACE。不触发流程创建、审批、余额等业务监听器。恢复后核对管理员报表、沈业然已办、流程时间线/审批意见和抄送详情；当前仅数据库层隔离验证，不声称生产恢复或真实界面验收通过。

## 删除入口审计

- 通用流程创建清理历史：已删除，确认本次根因。
- `BpmModelServiceImpl.cleanModel`：显式管理清理，有意删除目标模型历史；原查询仅 key 无租户，已修复隔离。不据此认定生产曾发生跨租户误删。
- `BpmTaskServiceImpl` 的运行实例取消：运行流程终止路径，未发现同编号历史扫描删除；与删除审批历史不同，未改业务取消语义。
- 模型删除调用 repositoryService.deleteModel 并停用已部署定义，未发现 cascade deleteDeployment 调用。
- 本次全仓 Java/YAML 搜索未发现业务定时清理审批历史调用。未覆盖数据库之外外部运维工具。

## 验证

先写 `CASES.md` 后实施。19个自动测试全部通过：真实Flowable历史保留2、范围服务10、真实MyBatis/H2 SQL与租户过滤5、模型清理租户2。Vue SFC脚本和模板编译通过，git diff --check通过。未声称全前端类型检查/浏览器验收；未部署测试或生产。

运行：`mvn -o -pl yudao-module-bpm/yudao-module-bpm-server -am test -Dtest=BpmLeaveReportScopeTest,BpmHistoryPreservationTest,BpmLeaveReportMapperTest,BpmModelCleanTenantTest -Dsurefire.failIfNoSpecifiedTests=false`。
